{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "lldb",
      "request": "launch",
      "name": "Tauri Development (Debug)",
      "cargo": {
        "args": ["tauri", "dev", "--context", "src-tauri"],
        "filter": { "name": "plano-corte-backend", "kind": "bin" }
      },
      "sourceLanguages": ["rust"],
      "cwd": "${workspaceFolder}"
    }
  ]
}
